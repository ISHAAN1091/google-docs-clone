# Client Side Google Docs Clone

To install the dependencies -

### `npm install`

To run the application in development mode -

### `npm start`

To build the application for production -

### `npm run build`
